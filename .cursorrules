You# ShipNative - Cursor Rules

You are working on **ShipNative**, a production-ready React Native boilerplate. This is a monorepo with three interconnected repositories that MUST stay in sync.

## ⚠️ START HERE
Read [AGENTS.md](./AGENTS.md) first. It is the primary entry point for all agents and points to the single source of truth ([AI_CONTEXT.md](./AI_CONTEXT.md)).

## 🔄 CRITICAL: Repository Synchronization
When modifying the boilerplate (`shipnativeapp/`), you MUST update documentation and the landing page:

### 1. Documentation (`mintlify_docs/docs/`)
- Update relevant `.mdx` files for any feature or architecture changes.
- Add new features to the index.

### 2. Landing Page (`landing_page/src/`)
- Update UI components (BentoGrid, Pricing) to reflect new capabilities.
- Update `CHANGELOG.md` and `ROADMAP.md`.

### 3. Internal Context (`vibe/` folder)
- Update `CONTEXT.md`, `TECH_STACK.md`, or `STYLE_GUIDE.md` as needed.

## 🎯 Tech Stack Summary (Detailed in AGENTS.md)
- **Styling**: Unistyles 3.0 ONLY.
- **Navigation**: React Navigation ONLY.
- **State**: Zustand + React Query.
- **Platform**: iOS, Android, and Web.

## 📱 Documentation Rules
- **New Features**: Create `docs/[FEATURE].md`, NOT in the root.
- **Root Files**: ONLY update existing files (`README.md`, `SUPABASE.md`, etc.).

---
*Refer to [AGENTS.md](./AGENTS.md) for full setup and coding patterns.*
