/**
 * Screen Layout Components
 *
 * These layouts provide consistent structure for different screen types.
 * Always use the appropriate layout for your screen type to ensure
 * visual consistency across the app.
 *
 * @example
 * // For auth screens (Login, Register, ForgotPassword, Welcome)
 * import { AuthScreenLayout } from '@/components/layouts'
 *
 * // For onboarding screens
 * import { OnboardingScreenLayout } from '@/components/layouts'
 */

export * from "./AuthScreenLayout"
export * from "./OnboardingScreenLayout"
